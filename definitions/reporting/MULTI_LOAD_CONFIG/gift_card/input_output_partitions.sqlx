config {
    type: "view",
    schema: "injenia",
    name: "gift_card_input_output_partitions"
}

SELECT DISTINCT EXTRACT(YEAR FROM data_vendita)
FROM ${ref("S_GIFT_CARD_*")}
WHERE DATE(CAST(ts_modifica AS TIMESTAMP)) = CURRENT_DATE