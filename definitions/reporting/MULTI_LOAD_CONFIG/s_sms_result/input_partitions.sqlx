config {
    type: "view",
    schema: "injenia",
    name: "s_sms_result_input_partitions"
}

SELECT DISTINCT PARSE_DATE("%Y%m%d", `partition`)
FROM ${ref("PARTIZIONI_LOADJOB")} 
WHERE 
	DATE(ts_lancio) = CURRENT_DATE 
AND table_name = 'S_LOG_SMS_RESULT'
		