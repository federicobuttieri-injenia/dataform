config {
    type: "view",
    schema: "injenia",
    name: "f_giacenza_input_partitions"
}

SELECT DISTINCT PARSE_DATE("%Y%m%d", `partition`)
FROM `mmfg-dwh-gruppo-prod.TEMP_LOCATION.PARTIZIONI_LOADJOB` 
WHERE 
	DATE(ts_lancio) = CURRENT_DATE 
AND table_name = 'S_GIACENZA'
		