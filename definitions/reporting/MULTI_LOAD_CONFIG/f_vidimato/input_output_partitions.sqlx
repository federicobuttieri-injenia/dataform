config {
    type: "view",
    schema: "DM_SELLIN",
    name: "f_vidimato_input_output_partitions"
}

SELECT DISTINCT data_vidimazione
FROM ${ref("DM_SELLOUT_STAGING", "S_APPROVAZIONE_ORDINE_*")}
WHERE DATE(ts_modifica) = CURRENT_DATE
		