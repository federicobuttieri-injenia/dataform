config {
    type: "view",
    schema: "injenia",
    name: "f_movimenti_contabilita_input_output_partitions"
}

SELECT DISTINCT(EXTRACT(YEAR FROM data_vendita))
FROM `mmfg-dwh-gruppo-prod.DM_SELLOUT_STAGING.S_MOVIMENTO_CONTABILITA_*`
WHERE DATE(CAST(ts_modifica AS TIMESTAMP)) = CURRENT_DATE
		