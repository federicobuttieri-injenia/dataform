config {
    type: "view",
    schema: "DM_CRM",
    name: "f_email_result_input_partitions"
}

SELECT DISTINCT PARSE_DATE("%Y%m%d", `partition`) AS value
FROM ${ref("TEMP_LOCATION", "PARTIZIONI_LOADJOB")} 
WHERE 
	DATE(ts_lancio) = CURRENT_DATE 
AND table_name = 'S_LOG_EMAIL_RESULT'
		