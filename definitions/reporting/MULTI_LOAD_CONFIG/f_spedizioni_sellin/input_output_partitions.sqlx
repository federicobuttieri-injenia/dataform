config {
    type: "view",
    schema: "DM_SELLIN",
    name: "f_spedizioni_sellin_input_output_partitions"
}

SELECT DISTINCT data_bolla
FROM ${ref("S_SPEDITO_*")}
WHERE DATE(ts_modifica) = CURRENT_DATE
		