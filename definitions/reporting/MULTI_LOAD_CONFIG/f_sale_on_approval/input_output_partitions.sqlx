config {
    type: "view",
    schema: "DM_SELLOUT",
    name: "f_sale_on_approval_input_output_partitions"
}

SELECT DISTINCT data_soa
FROM ${ref("LAST_MODIFIED_SOA_DATES")}
WHERE DATE(ts_inserimento) = CURRENT_DATE
		