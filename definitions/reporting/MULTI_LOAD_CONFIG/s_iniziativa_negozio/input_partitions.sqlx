config {
    type: "view",
    schema: "injenia",
    name: "s_iniziativa_negozio_input_partitions"
}

SELECT DISTINCT PARSE_DATE("%Y%m%d", `partition`)
FROM ${ref("TEMP_LOCATION", "PARTIZIONI_LOADJOB")}
WHERE
	DATE(ts_lancio) = CURRENT_DATE
AND table_name = 'S_LOG_INIZIATIVA_NEGOZIO'
		