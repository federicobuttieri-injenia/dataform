config {
    type: "incremental",
    schema: "injenia",
    name: "S_FIFO"
}

SELECT *
FROM `mmfg-dwh-gruppo-prod.DM_SELLOUT_STAGING.S_LOG_FIFO`
WHERE
	_PARTITIONDATE IN ({partitions})
AND data_carico IN ({where_partitions_current_table})
		