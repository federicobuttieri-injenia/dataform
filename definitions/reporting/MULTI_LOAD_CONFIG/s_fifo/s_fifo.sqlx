config {
    type: "incremental",
    schema: "injenia",
    name: "S_FIFO"
}

SELECT *
FROM ${ref("S_LOG_FIFO")}
WHERE
	_PARTITIONDATE IN ({partitions})
AND data_carico IN ({where_partitions_current_table})
		