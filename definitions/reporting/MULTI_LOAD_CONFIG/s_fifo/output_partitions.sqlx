config {
    type: "view",
    schema: "injenia",
    name: "s_fifo_output_partitions"
}

SELECT DISTINCT data_carico AS value
FROM `mmfg-dwh-gruppo-prod.DM_SELLOUT_STAGING.S_LOG_FIFO`
WHERE _PARTITIONDATE IN (SELECT * FROM ${ref("s_fifo_input_partitions")})
		