config {
    type: "view",
    schema: "injenia",
    name: "s_fatt_contab_input_output_partitions"
}

        SELECT DISTINCT DATE(daily_sales_date) AS daily_sales_date
        FROM `mmfg-dwh-gruppo-prod.DM_DIGITAL_STAGING.S_LOG_FATTURATO_CONTABILIZZATO`
        WHERE EXTRACT(DATE FROM _PARTITIONTIME) = CURRENT_DATE()
        