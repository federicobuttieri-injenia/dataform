config {
    type: "view",
    schema: "injenia",
    name: "file_vendite_esclusi_output_partitions"
}

SELECT DISTINCT data_documento AS value
FROM `mmfg-dwh-gruppo-prod.DM_SELLOUT_STAGING.S_FILE_VENDITE_ESCLUSI_BMS`
WHERE DATE(_PARTITIONTIME) IN (SELECT * FROM ${ref("file_vendite_esclusi_input_partitions")})
		